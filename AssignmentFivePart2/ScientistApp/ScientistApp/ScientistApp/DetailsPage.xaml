<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScientistApp.DetailsPage"
             Title="{Binding Name}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label" x:Key="ScientistInfo">
                <Setter Property="BackgroundColor" Value="White"/>
                <Setter Property="TextColor" Value="#252222"/>
                <Setter Property="FontAttributes" Value="None"/>
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView Orientation="Vertical">
            <StackLayout Padding="20">
                <Image Source="{Binding Image}" Aspect="AspectFit" HeightRequest="100" WidthRequest="100"/>
                <Label Text="{Binding Year}" Style="{StaticResource ScientistInfo}" FontAttributes="Italic" />
                <Label Text="{Binding Description}" Style="{StaticResource ScientistInfo}" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
    
</ContentPage>